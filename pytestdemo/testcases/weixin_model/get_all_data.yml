#查询标签
-   name: "查询所有标签"
    request:
        url: /tags/get
        method: get
        params:
            access_token: ${get_extra_data(access_token)}
    validate:
    -   eq: {status_code: 200}

